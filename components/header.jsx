import Nav from "./nav";
import SnsLinks from "./sns-links";

const Header = () => {
    return (
        <header>
            <Nav />
            <SnsLinks />
        </header>
    );
}

export default Header;